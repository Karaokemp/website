pipeline{
    agent any
    

    stages{
        stage("Choose Commit"){
            steps{
                 script {
                   values = ['Apple', 'Banana', 'Peach']
                   parameters = [[$class: 'BooleanParameterDefinition', defaultValue:false, description: '', name: it ]]
                   values.each {
                       echo it
                   }
 
                   res = input(
                       message: 'Was this successful?', parameters: parameters
                   )
                                                         print(res)
               }
 
        }
    }
    }
}